/* Од стандарден влез се вчитуваaт три броја кои ги означуваат времињата (во минути) со кои три натпреварувачи истрчале 5км на Скопскиот маратон.
Да се испечати редниот број на најбрзиот натпреварувач.
Доколку најбрзо време имаат повеќе од еден натпреварувач, се печатат редните броеви на сите такви натпреварувачи. */

#include <stdio.h>

int main()
{
    float x, y, z;
    scanf("%f %f %f", &x, &y, &z);
    if ((y>=x) && (z>=x)) {
    printf("1"); }
    if ((x>=y) && (z>=y)) {
    printf("2"); }
    if ((y>=z) && (x>=z)) {
    printf("3"); }
    return(0);
}
